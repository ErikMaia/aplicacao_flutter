import 'package:flutter/material.dart';

class DepartamentoEdit extends StatelessWidget {
  const DepartamentoEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}